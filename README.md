# aegis-rag
Project description Problem Statement: Modern enterprises store critical data in disconnected silos (PDFs, SQL databases, JSON logs). Employees need to query this information using natural language, but current AI tools either hallucinate answers or ignore strict security clearances, risking massive data leaks.
